name: JobTrack Runner Test

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check runner
        run: echo "GitHub Actions runner is working!"